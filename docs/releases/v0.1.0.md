# Release Notes: v0.1.0

## Status

Release-readiness candidate.

This release prepares the documentation-first foundation for review. It does not create a remote tag, publish a package, or deploy runtime software.

## Docs

Included:

- root repository documentation;
- architecture guides;
- engineering standards;
- ADRs;
- reference module documentation;
- kernel contract documentation;
- templates;
- examples;
- infrastructure guidance.

The README includes a documentation map so project-facing markdown files are reachable from the repository entry point.

## Contracts

Included machine-readable examples:

- [`contracts/openapi/public-api.v1.yml`](../../contracts/openapi/public-api.v1.yml)
- [`contracts/asyncapi/events.v1.yml`](../../contracts/asyncapi/events.v1.yml)
- [`contracts/json-schema/events/cloudevent-envelope.schema.json`](../../contracts/json-schema/events/cloudevent-envelope.schema.json)
- [`contracts/graphql/schema.graphql`](../../contracts/graphql/schema.graphql)
- [`contracts/protobuf/identity.proto`](../../contracts/protobuf/identity.proto)

The contracts are intentionally small and are examples of structure, versioning, and boundary discipline.

## Examples

Included examples cover:

- module communication;
- repository usage;
- RBAC policy evaluation;
- event handling;
- architecture fitness functions;
- framework mapping for Laravel, Go, .NET, and Node.js/TypeScript.

Examples are educational and framework-neutral unless explicitly marked as framework mapping guidance.

## Security

Included security coverage:

- authentication and authorization baseline;
- RBAC and ABAC-ready policy guidance;
- object-level authorization expectations;
- CSRF, JWT, OAuth, rate limit, audit, and logging guidance;
- CI expectations for secret, dependency, SAST, and container checks where applicable.

Known security limitation:

- v0.1 contains guidance and examples, not an executable security test suite.

## Runtime

Runtime status:

- no runnable application is included;
- no package manager project is included;
- no Docker Compose file is included;
- no Kubernetes manifest is included;
- no Terraform or cloud deployment automation is included.

The repository is an architecture foundation and educational reference, not a framework starter kit.

## Git Status

Prepared on branch:

```txt
docs/release-readiness-v0-1
```

Release readiness commit message:

```txt
docs: prepare v0.1 release readiness
```

No push, PR, merge, tag, release publication, or branch cleanup is implied by this release note.

## Known Limitations

- README reachability covers markdown documentation, not every non-markdown contract artifact.
- External standards references should be rechecked before a public release announcement.
- Framework adapter starter kits remain future work.
- Runtime validation is not applicable until a downstream implementation project exists.

## Verification Checklist

- [ ] README reachability check passes for repo-facing markdown files.
- [ ] Local markdown link check passes.
- [ ] Empty file check passes.
- [ ] Placeholder and agent-plan leak check passes.
- [ ] Release note contains docs, contracts, examples, security, runtime, and git status sections.
